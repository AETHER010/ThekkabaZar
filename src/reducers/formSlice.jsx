import { createSlice } from "@reduxjs/toolkit";

const formSlice = createSlice({
  name: "dropdetails",
  initialState: [],
  reducers: {
    getropdetails(state, action) {},
  },
});

export default formSlice.reducer;
